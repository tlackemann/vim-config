" Colors
" -----------

" Set neovim colors
if has('nvim') || has('termguicolors')
  set termguicolors
endif

colorscheme onedark

" Highlight the maxlength
set colorcolumn=100

let g:airline_theme = 'onedark'
